mixin cards(items)
    div.cards
        each item in items
            div(class='cards_item frame')
                h4=item.header
                img(src=item.icon alt="#")
                p=item.text