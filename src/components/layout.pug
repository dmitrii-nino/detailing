doctype html
html(lang="ru-RU")
    head
        meta(charset="utf-8")
        meta(http-equiv='X-UA-Compatible', content="IE=edge")
        meta(name="viewport" content="width=device-width,initial-scale=1")
        meta(name="keywords" content="")
        title='ПРЕМИАЛЬНЫЙ ДЕТЕЙЛИНГ'
        link(rel="preconnect" href="https://fonts.googleapis.com")
        link(rel="preconnect" href="https://fonts.gstatic.com" crossorigin)
        link(rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap")
        link(rel="stylesheet" type='text/css' href="./css/style.min.css")
        script(async)
            | const clientWidth = screen.width || window.innerWidth || document.documentElement.clientWidth || document.body.clientWidth;
        block head

        block indexVars
        block ceramicVars
        block filmVars
        block drycleaningVars
        block drycleaningPricesVars
        block toningVars
        block toningPricesVars

    body
        div.container
            include header

            include ../blocks/_libs
            block main

            include footer

            block modal

    script(src='./js/app.min.js')
